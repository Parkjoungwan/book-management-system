# API Key 및 환경변수 (절대 커밋 금지)
api_key.txt
.env
.env.local
.env*.local
book-server/config/application-secret.yml

# 로컬 DB (각자 테스트 데이터 충돌 방지 — db.sample.json을 복사해서 사용)
db.json

# ── Frontend ──────────────────────────────
node_modules/
dist/
dist-ssr/

# ── Backend (Spring Boot) ──────────────────
book-server/target/
book-server/.mvn/wrapper/maven-wrapper.jar
book-server/.idea/
book-server/*.iml
book-server/*.iws
book-server/*.ipr
*.mv.db
*.trace.db

# 실수로 들어간 self-gitlink (저장소 자기 자신 참조) 재발 방지
/4th_miniproject/

# ── 공통 ──────────────────────────────────
*.log
npm-debug.log*

# 에디터
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# PR 초안 (로컬 전용)
PR_*.md
